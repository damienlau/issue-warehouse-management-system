import shortcutModule from "./shortcut";

const state = () => ({});

const getters = {};

const actions = {};

const mutations = {};

const modules = { shortcutModule };

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules,
};
