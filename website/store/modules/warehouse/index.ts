import shortcut from "./shortcut";

const state = () => ({});

const getters = {};

const actions = {};

const mutations = {};

const modules = { shortcut };

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules,
};
